# [Deprecated Branch] GARbro

It is literally impossible to replace `BinaryFormatter` with json serialization without editing the scheme implementation in `ArcFormats` assembly, which is a fairly huge task and troublesome to maintain. Therefore, this branch is deprecated.